/** @type {import('next-sitemap').IConfig} */
module.exports = {
	siteUrl: "https://recodepros.dev", // Replace with your actual website URL
	generateRobotsTxt: true, // (optional) Generate a `robots.txt` file
	sitemapSize: 5000, // (optional) Split sitemaps after reaching a certain number of URLs
};
